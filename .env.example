# Backend API Server URL (Base URL for API requests)
# For local development, point it to your local express server port (usually http://localhost:5000)
# Leave it blank to fallback to Vite's dev proxy or same-domain deployment in production
VITE_API_URL=http://localhost:5000
